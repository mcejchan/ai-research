# Architecture simplification review checkpoint

- Run ID/time: `2026-07-31T0800Z-ai-research` / `2026-07-31T08:00:00Z`
- Project: `ai-research` (`/Users/michal/Projects/ai-research`)
- Question: After the prior cleanup, do remaining repository-level operation and knowledge-pipeline boundaries still encode competing authorities or misleading fallbacks?
- Verified evidence:
  - Root `Makefile` now owns the project gate and invokes quiz, viewer, and pipeline suites; root README names it authoritative.
  - Quiz deployment is path-scoped to `quiz/**`, runs its affected test, and deploys only `quiz/`: a legitimate deploy boundary, not a competing whole-repository test authority.
  - The repository gate completed successfully: quiz `10/10`, viewer `8/8`, pipeline `42/42` tests passed (captured run output).
  - `DriveClient` chooses Google Drive by ambient presence of `client_secret.json`; absence and every caught initialization/auth/service-build exception silently select local `mock_mode`.
  - In local mode, output root has a three-way precedence: `LOCAL_KB_ROOT`; otherwise path-like `DRIVE_FOLDER_ID`; otherwise repository `local-knowledge-base`. A direct execution probe confirmed all three selections.
  - `run_for_url` requires `DRIVE_FOLDER_ID` only in real Drive mode, so the same command/configuration can write remotely or locally depending on credentials and runtime failure.
  - Tests explicitly preserve auth-error-to-local fallback, but do not assert root selection or make the selected backend a caller-owned configuration value.
  - Repository and pipeline docs present the behavior as Drive/local mock fallback; historical smoke evidence shows a dummy remote-looking ID wrote locally to `local-knowledge-base`.
  - Direct knowledge-producing Claude commands also write under `local-knowledge-base`, but perform enrichment/indexing rather than transcript ingestion; this is a shared data boundary, not duplicate pipeline authority.
- Current conclusion:
  - One verified architectural finding: the storage adapter conflates backend choice, failure recovery, and output-root selection. This creates hidden runtime authority and a silent fail-open path from remote persistence to local persistence.
  - Documentation/test command divergence is real but subordinate: root `make test` is clearly repository authority; package-local `pytest` remains a valid component command.
- Next resumable trace: none required for this run. A future implementation discussion should choose an explicit storage contract (for example `STORAGE_BACKEND=drive|local` plus a dedicated local root) and decide whether Drive initialization failures should fail closed.
- continuable: false
