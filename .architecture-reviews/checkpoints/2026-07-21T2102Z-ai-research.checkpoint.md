# Architecture simplification review checkpoint

- Run ID/time: `2026-07-21T2102Z-ai-research` / `2026-07-21T21:02:00Z`
- Project: `ai-research` (`/Users/michal/Projects/ai-research`)
- Question: Does the repository have competing runtime/content authorities or avoidable fallback layers across its transcript, viewer, and quiz subsystems?
- Verified evidence:
  - Production quiz browser fetches only `levels/index.json` and level JSON; GitHub Pages workflow builds that index and uploads static `quiz/`.
  - `quiz/server.js` separately implements recursive level discovery and `/api/levels`/`/api/level`, but maintained code has no caller/reference to those endpoints; only stale files under `tmp/` describe the API.
  - Dynamic server metadata differs from deployed builder: server includes `index.json` and maps `extra-easy` to `hard`; emulation produced 21 rows for 20 source levels, 0 `extra-easy`, while the deployed manifest has 20/20 paths and 4 `extra-easy`.
  - Git history proves static migration commit `8b90375` changed browser calls from API to static files but retained the old server and dynamic metadata implementation.
  - Registry test command runs only pipeline `pytest`; maintained tests also exist in `quiz/build-index.test.js` and `yt-viewer/server.test.js`.
  - Actual Node test run: viewer test passed; quiz builder test failed (`weird` preserved but test expects `hard`) after commit `a578605` changed normalization to preserve `extra-easy` without updating the invalid-value contract/test.
  - Registry pipeline command is not self-contained: `pytest` is absent from PATH; `python3 -m pytest` collects but errors because `DRIVE_FOLDER_ID` is required at module import.
  - Deployment CI builds and deploys but runs no test. Recent visible deployment runs are green, including the commit that introduced the quiz test divergence.
  - `tmp/` contains tracked staging artifacts with no maintained callers and stale claims that `/api/levels` is the UI authority.
- Current conclusion:
  1. Remove the obsolete dynamic quiz server/metadata authority; retain static builder + level JSON as the single runtime path.
  2. Replace the pipeline-only, environment-dependent registry command with one project-level executable gate covering all three maintained test suites and explicit test-safe env/config handling; have deployment invoke the quiz portion or unified gate before upload.
  3. Remove tracked `tmp/` residue and ignore ephemeral staging output; canonical learnings already live under `learnings/`.
- Self-critique status:
  - Static manifest and source JSON are intentionally build artifact vs source, not competing authorities; no recommendation to collapse them.
  - Viewer server remains live and tested, unlike quiz server; no cross-app server merger proposed.
  - Transcript/Whisper and Drive/local fallbacks represent distinct capability/offline boundaries and were not shown to be redundant.
- Next resumable trace: none; synthesize report and ledger.
- continuable: false
