# Architecture simplification review checkpoint

- Run ID/time: `2026-08-29T080000Z` / `2026-08-29T08:00:00Z`
- Project: `ai-research`
- Question: Are repository health and quiz publication checks governed by one authority, and does the publication path fail closed on invalid content?
- Verified evidence:
  - Root `Makefile:test` is documented as the authoritative repository gate and runs quiz, viewer, knowledge-command, and Python pipeline suites.
  - `.github/workflows/deploy-quiz.yml` independently runs only `node --test quiz/build-index.test.js`, then `node quiz/build-index.js`, and publishes the resulting directory.
  - The workflow triggers only for `quiz/**` and its own file. This narrower boundary is legitimate for deployment speed, but the command is independently spelled rather than delegated to one quiz publish contract.
  - `quiz/build-index.js:50-56` catches every read/parse error, warns, and continues. Its CLI resolves successfully and writes an index after dropping bad inputs.
  - `quiz/build-index.test.js` has no malformed-input assertion. `quiz/validate-quiz.js` exists as a separate validator but neither the root gate nor deployment workflow invokes it.
  - Git history shows the partial deployment test was added in July after the original May tolerant builder; no later commit unified validation and publication.
- Current conclusion: Verified finding. Quiz acceptance has three divergent mechanisms—builder tests, tolerant index generation, and an unwired validator. The deployment authority can publish a partial catalog after malformed level input. Consolidate these behind one fail-closed quiz publish-check command used by both root tests and Pages.
- Rejected hypotheses:
  - A deployment-specific subset is not inherently wrong; running the entire Python/viewer repository gate before static Pages publication would increase unrelated coupling. The simplification should unify the quiz contract, not force every subsystem into deployment.
  - Ignored `tmp/` learnings are disposable agent staging, not a tracked second archive.
  - `plans/` and `learnings/` are intentional agent artifacts and were not treated as debt.
- Next resumable trail: none required; synthesize report and ledger.
- continuable: false
