# Architecture simplification review checkpoint

- Run ID/time: `2026-08-11T080000Z-ai-research` / `2026-08-11T08:00:00Z`
- Project: `ai-research` (`/Users/michal/Projects/ai-research`)
- Question/hypothesis: Does the YouTube ingestion/viewer flow have one truthful authority for analysis completion?
- Verified evidence:
  - `yt_pipeline.run_for_url` catches every analysis exception and writes a placeholder to the same `analysis_main.md` used for successful model output (`src/yt_pipeline.py:226-255`).
  - `test_yt_pipeline.py:253-279` intentionally requires that placeholder upload after an LLM exception.
  - `yt-viewer/server.js:77-81` defines `hasAnalysis` only by existence of `analysis_main.md`; both outcomes therefore report true.
  - `.claude/commands/{mindmap,kb-index,expand-analysis,flashcards}.md` and repository docs treat the filename as structured/LLM analysis. One tracked local knowledge item contains the placeholder, proving the hidden state occurs outside tests.
  - History shows the placeholder and viewer semantics came from separate commits and were never joined by an explicit completion contract.
  - Authoritative `make test` passed: quiz 2/2, viewer 1/1, pipeline 41/41. This verifies current behavior, including the misleading placeholder contract, rather than disproving the semantic issue.
  - Prior test/runtime/artifact hypotheses remain resolved or rejected; the July storage fallback finding remains prior history and is not duplicated here.
- Current conclusion: Verified finding — two semantically different producers share one filename while downstream code treats existence as completion. The smallest convergence is to reserve `analysis_main.md` for completed analysis and represent unavailable analysis by absence (viewer already understands absence), while keeping transcript ingestion successful. If operators need an explanation, use logging/retry metadata outside the product-content contract rather than a second content-shaped artifact.
- Self-critique: Treating analysis failure as fatal would unnecessarily couple transcript capture to an optional enrichment and reduce availability. Adding a manifest/state machine would add authority and lifecycle states. Reserving the existing filename and existing absence state removes the lie without either tradeoff; consumer commands already have at least some missing-file handling, though all should be contract-tested before implementation.
- Continuable next trace: none; report and ledger are ready to write.
- continuable: false
